const calculator = {
    add: (a, b) => a + b,
    subtract: (a, b) => a - b,
    multiply: (a, b) => a * b,
    divide: (a, b) => a / b,
  };
  
  const result = calculator.multiply(5, 2); // 10
  const result2 = calculator.divide(10, 2); // 5
  const result3 = calculator.add(5, 2); // 7
  const result4 = calculator.subtract(5, 2); // 3